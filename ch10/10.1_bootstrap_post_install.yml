---
- hosts: all
  become: true

  tasks:
    - name: install packages
      package:
        state: latest
        name:
          - nmap
          - vsftpd
      when: ansible_distribution in ["Fedora", "Ubuntu"]

